# placeholder for learned transition model configuration

episode_days: 90
steps_per_day: 5
seed: 42

initial_state:
  activity_level: sedentary

state:
  schema: heartsteps

actions:
  schema: heartsteps

reward:
  factor: activity_level
  values:
    sedentary: 0.0
    active: 1.0

transition_model:
  type: learned
  model: monte-carlo
  schema: heartsteps
